# DEPRECATED: nginx is now defined in ../docker-compose.prod.yml and deployed
# via the PRODUCTION DEPLOY workflow. This file is kept for reference only.
#
# Certbot renewal paths have moved to:
#   /home/ubuntu/micro_ai/nginx/certbot/conf
#   /home/ubuntu/micro_ai/nginx/certbot/www
#
# See nginx/README.md for SSL renewal instructions.
