# DEPRECATED: nginx is deployed via docker-compose.prod.yml in the PRODUCTION DEPLOY workflow.
# This workflow is disabled and kept only so existing links/bookmarks show a clear message.
name: NGINX (Prod Server) [DEPRECATED]

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow moved
        run: |
          echo "::error::This workflow is deprecated. Nginx is now deployed as part of PRODUCTION DEPLOY(Prod Server)."
          exit 1
