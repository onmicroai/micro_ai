name: Postgresql+Redis Deployment

on:
  workflow_dispatch:
    inputs:
      force_recreate:
        description: 'Force Recreate?'
        required: false
        default: 'false'
        type: boolean
      containers:
        description: 'Containers to Up/Recreate'
        required: true
        default: 'both'
        type: choice
        options:
          - db
          - redis
          - both

jobs:
  recreate_dbs:
    name: Recreate Databases
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Build Web
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP_DEV }}
          username: 'ubuntu'
          key: ${{ secrets.SSH_PRIVATE_KEY_DEV }}
          port: 22
          script: |
            cd /home/ubuntu/micro_ai
            git pull origin dev
            
            # ensure db and redis are up
            FORCE_RECREATE=""
            if [ "${{ inputs.force_recreate }}" = "true" ]; then
             FORCE_RECREATE="--force-recreate"
            fi
  
            CONTAINERS=""
            if [ "${{ inputs.containers }}" = "both" ]; then
              CONTAINERS="redis db"
            else
              CONTAINERS="${{ inputs.containers }}"
            fi
  
            docker-compose -p backend -f docker-compose.dev.yml up -d $FORCE_RECREATE $CONTAINERS
